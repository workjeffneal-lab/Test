# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty (no commits, no source files). There is no
existing codebase, build system, test suite, or documented convention to
describe yet.

## Next steps for future instances

Once this repository has real content, update this file to include:

1. **Commands** — how to install dependencies, build, lint, run tests
   (including running a single test), and start the project locally.
2. **Architecture** — the high-level structure of the codebase: major
   modules/services, how they relate, and any non-obvious design decisions
   that span multiple files.
3. Any conventions pulled from a `README.md`, `.cursor/rules/`,
   `.cursorrules`, or `.github/copilot-instructions.md` if those are added.

Do not fill in the sections above with generic or speculative content —
regenerate this file from the actual codebase once one exists (e.g. via the
`init` skill/command).
